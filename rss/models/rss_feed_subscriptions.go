// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// RSSFeedSubscription is an object representing the database table.
type RSSFeedSubscription struct {
	ID              int              `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt       time.Time        `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time        `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	GuildID         int64            `boil:"guild_id" json:"guild_id" toml:"guild_id" yaml:"guild_id"`
	ChannelID       int64            `boil:"channel_id" json:"channel_id" toml:"channel_id" yaml:"channel_id"`
	FeedURL         string           `boil:"feed_url" json:"feed_url" toml:"feed_url" yaml:"feed_url"`
	MentionEveryone bool             `boil:"mention_everyone" json:"mention_everyone" toml:"mention_everyone" yaml:"mention_everyone"`
	MentionRoles    types.Int64Array `boil:"mention_roles" json:"mention_roles,omitempty" toml:"mention_roles" yaml:"mention_roles,omitempty"`
	Enabled         bool             `boil:"enabled" json:"enabled" toml:"enabled" yaml:"enabled"`

	R *rssFeedSubscriptionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L rssFeedSubscriptionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RSSFeedSubscriptionColumns = struct {
	ID              string
	CreatedAt       string
	UpdatedAt       string
	GuildID         string
	ChannelID       string
	FeedURL         string
	MentionEveryone string
	MentionRoles    string
	Enabled         string
}{
	ID:              "id",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
	GuildID:         "guild_id",
	ChannelID:       "channel_id",
	FeedURL:         "feed_url",
	MentionEveryone: "mention_everyone",
	MentionRoles:    "mention_roles",
	Enabled:         "enabled",
}

var RSSFeedSubscriptionTableColumns = struct {
	ID              string
	CreatedAt       string
	UpdatedAt       string
	GuildID         string
	ChannelID       string
	FeedURL         string
	MentionEveryone string
	MentionRoles    string
	Enabled         string
}{
	ID:              "rss_feed_subscriptions.id",
	CreatedAt:       "rss_feed_subscriptions.created_at",
	UpdatedAt:       "rss_feed_subscriptions.updated_at",
	GuildID:         "rss_feed_subscriptions.guild_id",
	ChannelID:       "rss_feed_subscriptions.channel_id",
	FeedURL:         "rss_feed_subscriptions.feed_url",
	MentionEveryone: "rss_feed_subscriptions.mention_everyone",
	MentionRoles:    "rss_feed_subscriptions.mention_roles",
	Enabled:         "rss_feed_subscriptions.enabled",
}

// Generated where

type whereHelperint struct{ field string }

func (w whereHelperint) EQ(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint) NEQ(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint) LT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint) LTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint) GT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint) GTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint) IN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint) NIN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperstring struct{ field string }

func (w whereHelperstring) EQ(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperstring) NEQ(x string) qm.QueryMod    { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperstring) LT(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperstring) LTE(x string) qm.QueryMod    { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperstring) GT(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperstring) GTE(x string) qm.QueryMod    { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperstring) LIKE(x string) qm.QueryMod   { return qm.Where(w.field+" LIKE ?", x) }
func (w whereHelperstring) NLIKE(x string) qm.QueryMod  { return qm.Where(w.field+" NOT LIKE ?", x) }
func (w whereHelperstring) ILIKE(x string) qm.QueryMod  { return qm.Where(w.field+" ILIKE ?", x) }
func (w whereHelperstring) NILIKE(x string) qm.QueryMod { return qm.Where(w.field+" NOT ILIKE ?", x) }
func (w whereHelperstring) IN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperstring) NIN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperbool struct{ field string }

func (w whereHelperbool) EQ(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperbool) NEQ(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperbool) LT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperbool) LTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperbool) GT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperbool) GTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }

type whereHelpertypes_Int64Array struct{ field string }

func (w whereHelpertypes_Int64Array) EQ(x types.Int64Array) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpertypes_Int64Array) NEQ(x types.Int64Array) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpertypes_Int64Array) LT(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_Int64Array) LTE(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_Int64Array) GT(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_Int64Array) GTE(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpertypes_Int64Array) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpertypes_Int64Array) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var RSSFeedSubscriptionWhere = struct {
	ID              whereHelperint
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
	GuildID         whereHelperint64
	ChannelID       whereHelperint64
	FeedURL         whereHelperstring
	MentionEveryone whereHelperbool
	MentionRoles    whereHelpertypes_Int64Array
	Enabled         whereHelperbool
}{
	ID:              whereHelperint{field: "\"rss_feed_subscriptions\".\"id\""},
	CreatedAt:       whereHelpertime_Time{field: "\"rss_feed_subscriptions\".\"created_at\""},
	UpdatedAt:       whereHelpertime_Time{field: "\"rss_feed_subscriptions\".\"updated_at\""},
	GuildID:         whereHelperint64{field: "\"rss_feed_subscriptions\".\"guild_id\""},
	ChannelID:       whereHelperint64{field: "\"rss_feed_subscriptions\".\"channel_id\""},
	FeedURL:         whereHelperstring{field: "\"rss_feed_subscriptions\".\"feed_url\""},
	MentionEveryone: whereHelperbool{field: "\"rss_feed_subscriptions\".\"mention_everyone\""},
	MentionRoles:    whereHelpertypes_Int64Array{field: "\"rss_feed_subscriptions\".\"mention_roles\""},
	Enabled:         whereHelperbool{field: "\"rss_feed_subscriptions\".\"enabled\""},
}

// RSSFeedSubscriptionRels is where relationship names are stored.
var RSSFeedSubscriptionRels = struct {
}{}

// rssFeedSubscriptionR is where relationships are stored.
type rssFeedSubscriptionR struct {
}

// NewStruct creates a new relationship struct
func (*rssFeedSubscriptionR) NewStruct() *rssFeedSubscriptionR {
	return &rssFeedSubscriptionR{}
}

// rssFeedSubscriptionL is where Load methods for each relationship are stored.
type rssFeedSubscriptionL struct{}

var (
	rssFeedSubscriptionAllColumns            = []string{"id", "created_at", "updated_at", "guild_id", "channel_id", "feed_url", "mention_everyone", "mention_roles", "enabled"}
	rssFeedSubscriptionColumnsWithoutDefault = []string{"created_at", "updated_at", "guild_id", "channel_id", "feed_url", "mention_everyone"}
	rssFeedSubscriptionColumnsWithDefault    = []string{"id", "mention_roles", "enabled"}
	rssFeedSubscriptionPrimaryKeyColumns     = []string{"id"}
	rssFeedSubscriptionGeneratedColumns      = []string{}
)

type (
	// RSSFeedSubscriptionSlice is an alias for a slice of pointers to RSSFeedSubscription.
	// This should almost always be used instead of []RSSFeedSubscription.
	RSSFeedSubscriptionSlice []*RSSFeedSubscription

	rssFeedSubscriptionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	rssFeedSubscriptionType                 = reflect.TypeOf(&RSSFeedSubscription{})
	rssFeedSubscriptionMapping              = queries.MakeStructMapping(rssFeedSubscriptionType)
	rssFeedSubscriptionPrimaryKeyMapping, _ = queries.BindMapping(rssFeedSubscriptionType, rssFeedSubscriptionMapping, rssFeedSubscriptionPrimaryKeyColumns)
	rssFeedSubscriptionInsertCacheMut       sync.RWMutex
	rssFeedSubscriptionInsertCache          = make(map[string]insertCache)
	rssFeedSubscriptionUpdateCacheMut       sync.RWMutex
	rssFeedSubscriptionUpdateCache          = make(map[string]updateCache)
	rssFeedSubscriptionUpsertCacheMut       sync.RWMutex
	rssFeedSubscriptionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single rssFeedSubscription record from the query using the global executor.
func (q rssFeedSubscriptionQuery) OneG(ctx context.Context) (*RSSFeedSubscription, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single rssFeedSubscription record from the query.
func (q rssFeedSubscriptionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RSSFeedSubscription, error) {
	o := &RSSFeedSubscription{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for rss_feed_subscriptions")
	}

	return o, nil
}

// AllG returns all RSSFeedSubscription records from the query using the global executor.
func (q rssFeedSubscriptionQuery) AllG(ctx context.Context) (RSSFeedSubscriptionSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all RSSFeedSubscription records from the query.
func (q rssFeedSubscriptionQuery) All(ctx context.Context, exec boil.ContextExecutor) (RSSFeedSubscriptionSlice, error) {
	var o []*RSSFeedSubscription

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RSSFeedSubscription slice")
	}

	return o, nil
}

// CountG returns the count of all RSSFeedSubscription records in the query using the global executor
func (q rssFeedSubscriptionQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all RSSFeedSubscription records in the query.
func (q rssFeedSubscriptionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count rss_feed_subscriptions rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q rssFeedSubscriptionQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q rssFeedSubscriptionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if rss_feed_subscriptions exists")
	}

	return count > 0, nil
}

// RSSFeedSubscriptions retrieves all the records using an executor.
func RSSFeedSubscriptions(mods ...qm.QueryMod) rssFeedSubscriptionQuery {
	mods = append(mods, qm.From("\"rss_feed_subscriptions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"rss_feed_subscriptions\".*"})
	}

	return rssFeedSubscriptionQuery{q}
}

// FindRSSFeedSubscriptionG retrieves a single record by ID.
func FindRSSFeedSubscriptionG(ctx context.Context, iD int, selectCols ...string) (*RSSFeedSubscription, error) {
	return FindRSSFeedSubscription(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindRSSFeedSubscription retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRSSFeedSubscription(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*RSSFeedSubscription, error) {
	rssFeedSubscriptionObj := &RSSFeedSubscription{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"rss_feed_subscriptions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, rssFeedSubscriptionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from rss_feed_subscriptions")
	}

	return rssFeedSubscriptionObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *RSSFeedSubscription) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RSSFeedSubscription) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no rss_feed_subscriptions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(rssFeedSubscriptionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	rssFeedSubscriptionInsertCacheMut.RLock()
	cache, cached := rssFeedSubscriptionInsertCache[key]
	rssFeedSubscriptionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			rssFeedSubscriptionAllColumns,
			rssFeedSubscriptionColumnsWithDefault,
			rssFeedSubscriptionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(rssFeedSubscriptionType, rssFeedSubscriptionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(rssFeedSubscriptionType, rssFeedSubscriptionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"rss_feed_subscriptions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"rss_feed_subscriptions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into rss_feed_subscriptions")
	}

	if !cached {
		rssFeedSubscriptionInsertCacheMut.Lock()
		rssFeedSubscriptionInsertCache[key] = cache
		rssFeedSubscriptionInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single RSSFeedSubscription record using the global executor.
// See Update for more documentation.
func (o *RSSFeedSubscription) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the RSSFeedSubscription.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RSSFeedSubscription) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	key := makeCacheKey(columns, nil)
	rssFeedSubscriptionUpdateCacheMut.RLock()
	cache, cached := rssFeedSubscriptionUpdateCache[key]
	rssFeedSubscriptionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			rssFeedSubscriptionAllColumns,
			rssFeedSubscriptionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update rss_feed_subscriptions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"rss_feed_subscriptions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, rssFeedSubscriptionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(rssFeedSubscriptionType, rssFeedSubscriptionMapping, append(wl, rssFeedSubscriptionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update rss_feed_subscriptions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for rss_feed_subscriptions")
	}

	if !cached {
		rssFeedSubscriptionUpdateCacheMut.Lock()
		rssFeedSubscriptionUpdateCache[key] = cache
		rssFeedSubscriptionUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q rssFeedSubscriptionQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q rssFeedSubscriptionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for rss_feed_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for rss_feed_subscriptions")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o RSSFeedSubscriptionSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RSSFeedSubscriptionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), rssFeedSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"rss_feed_subscriptions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, rssFeedSubscriptionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in rssFeedSubscription slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all rssFeedSubscription")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *RSSFeedSubscription) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RSSFeedSubscription) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no rss_feed_subscriptions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	nzDefaults := queries.NonZeroDefaultSet(rssFeedSubscriptionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	rssFeedSubscriptionUpsertCacheMut.RLock()
	cache, cached := rssFeedSubscriptionUpsertCache[key]
	rssFeedSubscriptionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			rssFeedSubscriptionAllColumns,
			rssFeedSubscriptionColumnsWithDefault,
			rssFeedSubscriptionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			rssFeedSubscriptionAllColumns,
			rssFeedSubscriptionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert rss_feed_subscriptions, could not build update column list")
		}

		ret := strmangle.SetComplement(rssFeedSubscriptionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(rssFeedSubscriptionPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert rss_feed_subscriptions, could not build conflict column list")
			}

			conflict = make([]string, len(rssFeedSubscriptionPrimaryKeyColumns))
			copy(conflict, rssFeedSubscriptionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"rss_feed_subscriptions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(rssFeedSubscriptionType, rssFeedSubscriptionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(rssFeedSubscriptionType, rssFeedSubscriptionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert rss_feed_subscriptions")
	}

	if !cached {
		rssFeedSubscriptionUpsertCacheMut.Lock()
		rssFeedSubscriptionUpsertCache[key] = cache
		rssFeedSubscriptionUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single RSSFeedSubscription record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *RSSFeedSubscription) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single RSSFeedSubscription record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RSSFeedSubscription) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RSSFeedSubscription provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), rssFeedSubscriptionPrimaryKeyMapping)
	sql := "DELETE FROM \"rss_feed_subscriptions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from rss_feed_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for rss_feed_subscriptions")
	}

	return rowsAff, nil
}

func (q rssFeedSubscriptionQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q rssFeedSubscriptionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no rssFeedSubscriptionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from rss_feed_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for rss_feed_subscriptions")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o RSSFeedSubscriptionSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RSSFeedSubscriptionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), rssFeedSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"rss_feed_subscriptions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, rssFeedSubscriptionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from rssFeedSubscription slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for rss_feed_subscriptions")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *RSSFeedSubscription) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no RSSFeedSubscription provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RSSFeedSubscription) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRSSFeedSubscription(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RSSFeedSubscriptionSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty RSSFeedSubscriptionSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RSSFeedSubscriptionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RSSFeedSubscriptionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), rssFeedSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"rss_feed_subscriptions\".* FROM \"rss_feed_subscriptions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, rssFeedSubscriptionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RSSFeedSubscriptionSlice")
	}

	*o = slice

	return nil
}

// RSSFeedSubscriptionExistsG checks if the RSSFeedSubscription row exists.
func RSSFeedSubscriptionExistsG(ctx context.Context, iD int) (bool, error) {
	return RSSFeedSubscriptionExists(ctx, boil.GetContextDB(), iD)
}

// RSSFeedSubscriptionExists checks if the RSSFeedSubscription row exists.
func RSSFeedSubscriptionExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"rss_feed_subscriptions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if rss_feed_subscriptions exists")
	}

	return exists, nil
}

// Exists checks if the RSSFeedSubscription row exists.
func (o *RSSFeedSubscription) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return RSSFeedSubscriptionExists(ctx, exec, o.ID)
}
