// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testJoinedGuilds(t *testing.T) {
	t.Parallel()

	query := JoinedGuilds()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testJoinedGuildsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testJoinedGuildsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := JoinedGuilds().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testJoinedGuildsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := JoinedGuildSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testJoinedGuildsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := JoinedGuildExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if JoinedGuild exists: %s", err)
	}
	if !e {
		t.Errorf("Expected JoinedGuildExists to return true, but got false.")
	}
}

func testJoinedGuildsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	joinedGuildFound, err := FindJoinedGuild(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if joinedGuildFound == nil {
		t.Error("want a record, got nil")
	}
}

func testJoinedGuildsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = JoinedGuilds().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testJoinedGuildsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := JoinedGuilds().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testJoinedGuildsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	joinedGuildOne := &JoinedGuild{}
	joinedGuildTwo := &JoinedGuild{}
	if err = randomize.Struct(seed, joinedGuildOne, joinedGuildDBTypes, false, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}
	if err = randomize.Struct(seed, joinedGuildTwo, joinedGuildDBTypes, false, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = joinedGuildOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = joinedGuildTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := JoinedGuilds().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testJoinedGuildsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	joinedGuildOne := &JoinedGuild{}
	joinedGuildTwo := &JoinedGuild{}
	if err = randomize.Struct(seed, joinedGuildOne, joinedGuildDBTypes, false, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}
	if err = randomize.Struct(seed, joinedGuildTwo, joinedGuildDBTypes, false, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = joinedGuildOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = joinedGuildTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testJoinedGuildsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testJoinedGuildsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(joinedGuildColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testJoinedGuildsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testJoinedGuildsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := JoinedGuildSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testJoinedGuildsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := JoinedGuilds().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	joinedGuildDBTypes = map[string]string{`Avatar`: `text`, `ID`: `bigint`, `JoinedAt`: `timestamp with time zone`, `LeftAt`: `timestamp with time zone`, `MemberCount`: `bigint`, `Name`: `text`, `OwnerID`: `bigint`}
	_                  = bytes.MinRead
)

func testJoinedGuildsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(joinedGuildPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(joinedGuildColumns) == len(joinedGuildPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testJoinedGuildsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(joinedGuildColumns) == len(joinedGuildPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &JoinedGuild{}
	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, joinedGuildDBTypes, true, joinedGuildPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(joinedGuildColumns, joinedGuildPrimaryKeyColumns) {
		fields = joinedGuildColumns
	} else {
		fields = strmangle.SetComplement(
			joinedGuildColumns,
			joinedGuildPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := JoinedGuildSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testJoinedGuildsUpsert(t *testing.T) {
	t.Parallel()

	if len(joinedGuildColumns) == len(joinedGuildPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := JoinedGuild{}
	if err = randomize.Struct(seed, &o, joinedGuildDBTypes, true); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert JoinedGuild: %s", err)
	}

	count, err := JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, joinedGuildDBTypes, false, joinedGuildPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize JoinedGuild struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert JoinedGuild: %s", err)
	}

	count, err = JoinedGuilds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
