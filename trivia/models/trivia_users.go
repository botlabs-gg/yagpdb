// Code generated by SQLBoiler 4.19.5 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TriviaUser is an object representing the database table.
type TriviaUser struct {
	UserID           int64     `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	GuildID          int64     `boil:"guild_id" json:"guild_id" toml:"guild_id" yaml:"guild_id"`
	Score            int       `boil:"score" json:"score" toml:"score" yaml:"score"`
	CorrectAnswers   int       `boil:"correct_answers" json:"correct_answers" toml:"correct_answers" yaml:"correct_answers"`
	IncorrectAnswers int       `boil:"incorrect_answers" json:"incorrect_answers" toml:"incorrect_answers" yaml:"incorrect_answers"`
	CurrentStreak    int       `boil:"current_streak" json:"current_streak" toml:"current_streak" yaml:"current_streak"`
	MaxStreak        int       `boil:"max_streak" json:"max_streak" toml:"max_streak" yaml:"max_streak"`
	LastPlayed       time.Time `boil:"last_played" json:"last_played" toml:"last_played" yaml:"last_played"`

	R *triviaUserR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L triviaUserL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TriviaUserColumns = struct {
	UserID           string
	GuildID          string
	Score            string
	CorrectAnswers   string
	IncorrectAnswers string
	CurrentStreak    string
	MaxStreak        string
	LastPlayed       string
}{
	UserID:           "user_id",
	GuildID:          "guild_id",
	Score:            "score",
	CorrectAnswers:   "correct_answers",
	IncorrectAnswers: "incorrect_answers",
	CurrentStreak:    "current_streak",
	MaxStreak:        "max_streak",
	LastPlayed:       "last_played",
}

var TriviaUserTableColumns = struct {
	UserID           string
	GuildID          string
	Score            string
	CorrectAnswers   string
	IncorrectAnswers string
	CurrentStreak    string
	MaxStreak        string
	LastPlayed       string
}{
	UserID:           "trivia_users.user_id",
	GuildID:          "trivia_users.guild_id",
	Score:            "trivia_users.score",
	CorrectAnswers:   "trivia_users.correct_answers",
	IncorrectAnswers: "trivia_users.incorrect_answers",
	CurrentStreak:    "trivia_users.current_streak",
	MaxStreak:        "trivia_users.max_streak",
	LastPlayed:       "trivia_users.last_played",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperint struct{ field string }

func (w whereHelperint) EQ(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint) NEQ(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint) LT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint) LTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint) GT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint) GTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint) IN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint) NIN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var TriviaUserWhere = struct {
	UserID           whereHelperint64
	GuildID          whereHelperint64
	Score            whereHelperint
	CorrectAnswers   whereHelperint
	IncorrectAnswers whereHelperint
	CurrentStreak    whereHelperint
	MaxStreak        whereHelperint
	LastPlayed       whereHelpertime_Time
}{
	UserID:           whereHelperint64{field: "\"trivia_users\".\"user_id\""},
	GuildID:          whereHelperint64{field: "\"trivia_users\".\"guild_id\""},
	Score:            whereHelperint{field: "\"trivia_users\".\"score\""},
	CorrectAnswers:   whereHelperint{field: "\"trivia_users\".\"correct_answers\""},
	IncorrectAnswers: whereHelperint{field: "\"trivia_users\".\"incorrect_answers\""},
	CurrentStreak:    whereHelperint{field: "\"trivia_users\".\"current_streak\""},
	MaxStreak:        whereHelperint{field: "\"trivia_users\".\"max_streak\""},
	LastPlayed:       whereHelpertime_Time{field: "\"trivia_users\".\"last_played\""},
}

// TriviaUserRels is where relationship names are stored.
var TriviaUserRels = struct {
}{}

// triviaUserR is where relationships are stored.
type triviaUserR struct {
}

// NewStruct creates a new relationship struct
func (*triviaUserR) NewStruct() *triviaUserR {
	return &triviaUserR{}
}

// triviaUserL is where Load methods for each relationship are stored.
type triviaUserL struct{}

var (
	triviaUserAllColumns            = []string{"user_id", "guild_id", "score", "correct_answers", "incorrect_answers", "current_streak", "max_streak", "last_played"}
	triviaUserColumnsWithoutDefault = []string{"user_id", "guild_id", "last_played"}
	triviaUserColumnsWithDefault    = []string{"score", "correct_answers", "incorrect_answers", "current_streak", "max_streak"}
	triviaUserPrimaryKeyColumns     = []string{"guild_id", "user_id"}
	triviaUserGeneratedColumns      = []string{}
)

type (
	// TriviaUserSlice is an alias for a slice of pointers to TriviaUser.
	// This should almost always be used instead of []TriviaUser.
	TriviaUserSlice []*TriviaUser

	triviaUserQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	triviaUserType                 = reflect.TypeOf(&TriviaUser{})
	triviaUserMapping              = queries.MakeStructMapping(triviaUserType)
	triviaUserPrimaryKeyMapping, _ = queries.BindMapping(triviaUserType, triviaUserMapping, triviaUserPrimaryKeyColumns)
	triviaUserInsertCacheMut       sync.RWMutex
	triviaUserInsertCache          = make(map[string]insertCache)
	triviaUserUpdateCacheMut       sync.RWMutex
	triviaUserUpdateCache          = make(map[string]updateCache)
	triviaUserUpsertCacheMut       sync.RWMutex
	triviaUserUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single triviaUser record from the query using the global executor.
func (q triviaUserQuery) OneG(ctx context.Context) (*TriviaUser, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single triviaUser record from the query.
func (q triviaUserQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TriviaUser, error) {
	o := &TriviaUser{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for trivia_users")
	}

	return o, nil
}

// AllG returns all TriviaUser records from the query using the global executor.
func (q triviaUserQuery) AllG(ctx context.Context) (TriviaUserSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all TriviaUser records from the query.
func (q triviaUserQuery) All(ctx context.Context, exec boil.ContextExecutor) (TriviaUserSlice, error) {
	var o []*TriviaUser

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TriviaUser slice")
	}

	return o, nil
}

// CountG returns the count of all TriviaUser records in the query using the global executor
func (q triviaUserQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all TriviaUser records in the query.
func (q triviaUserQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count trivia_users rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q triviaUserQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q triviaUserQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if trivia_users exists")
	}

	return count > 0, nil
}

// TriviaUsers retrieves all the records using an executor.
func TriviaUsers(mods ...qm.QueryMod) triviaUserQuery {
	mods = append(mods, qm.From("\"trivia_users\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"trivia_users\".*"})
	}

	return triviaUserQuery{q}
}

// FindTriviaUserG retrieves a single record by ID.
func FindTriviaUserG(ctx context.Context, guildID int64, userID int64, selectCols ...string) (*TriviaUser, error) {
	return FindTriviaUser(ctx, boil.GetContextDB(), guildID, userID, selectCols...)
}

// FindTriviaUser retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTriviaUser(ctx context.Context, exec boil.ContextExecutor, guildID int64, userID int64, selectCols ...string) (*TriviaUser, error) {
	triviaUserObj := &TriviaUser{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"trivia_users\" where \"guild_id\"=$1 AND \"user_id\"=$2", sel,
	)

	q := queries.Raw(query, guildID, userID)

	err := q.Bind(ctx, exec, triviaUserObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from trivia_users")
	}

	return triviaUserObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TriviaUser) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TriviaUser) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no trivia_users provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(triviaUserColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	triviaUserInsertCacheMut.RLock()
	cache, cached := triviaUserInsertCache[key]
	triviaUserInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			triviaUserAllColumns,
			triviaUserColumnsWithDefault,
			triviaUserColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(triviaUserType, triviaUserMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(triviaUserType, triviaUserMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"trivia_users\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"trivia_users\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into trivia_users")
	}

	if !cached {
		triviaUserInsertCacheMut.Lock()
		triviaUserInsertCache[key] = cache
		triviaUserInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single TriviaUser record using the global executor.
// See Update for more documentation.
func (o *TriviaUser) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the TriviaUser.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TriviaUser) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	triviaUserUpdateCacheMut.RLock()
	cache, cached := triviaUserUpdateCache[key]
	triviaUserUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			triviaUserAllColumns,
			triviaUserPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update trivia_users, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"trivia_users\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, triviaUserPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(triviaUserType, triviaUserMapping, append(wl, triviaUserPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update trivia_users row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for trivia_users")
	}

	if !cached {
		triviaUserUpdateCacheMut.Lock()
		triviaUserUpdateCache[key] = cache
		triviaUserUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q triviaUserQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q triviaUserQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for trivia_users")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for trivia_users")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TriviaUserSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TriviaUserSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), triviaUserPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"trivia_users\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, triviaUserPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in triviaUser slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all triviaUser")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TriviaUser) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TriviaUser) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no trivia_users provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(triviaUserColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	triviaUserUpsertCacheMut.RLock()
	cache, cached := triviaUserUpsertCache[key]
	triviaUserUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			triviaUserAllColumns,
			triviaUserColumnsWithDefault,
			triviaUserColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			triviaUserAllColumns,
			triviaUserPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert trivia_users, could not build update column list")
		}

		ret := strmangle.SetComplement(triviaUserAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(triviaUserPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert trivia_users, could not build conflict column list")
			}

			conflict = make([]string, len(triviaUserPrimaryKeyColumns))
			copy(conflict, triviaUserPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"trivia_users\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(triviaUserType, triviaUserMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(triviaUserType, triviaUserMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert trivia_users")
	}

	if !cached {
		triviaUserUpsertCacheMut.Lock()
		triviaUserUpsertCache[key] = cache
		triviaUserUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single TriviaUser record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TriviaUser) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single TriviaUser record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TriviaUser) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TriviaUser provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), triviaUserPrimaryKeyMapping)
	sql := "DELETE FROM \"trivia_users\" WHERE \"guild_id\"=$1 AND \"user_id\"=$2"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from trivia_users")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for trivia_users")
	}

	return rowsAff, nil
}

func (q triviaUserQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q triviaUserQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no triviaUserQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from trivia_users")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for trivia_users")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TriviaUserSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TriviaUserSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), triviaUserPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"trivia_users\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, triviaUserPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from triviaUser slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for trivia_users")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TriviaUser) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no TriviaUser provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TriviaUser) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTriviaUser(ctx, exec, o.GuildID, o.UserID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TriviaUserSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty TriviaUserSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TriviaUserSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TriviaUserSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), triviaUserPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"trivia_users\".* FROM \"trivia_users\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, triviaUserPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TriviaUserSlice")
	}

	*o = slice

	return nil
}

// TriviaUserExistsG checks if the TriviaUser row exists.
func TriviaUserExistsG(ctx context.Context, guildID int64, userID int64) (bool, error) {
	return TriviaUserExists(ctx, boil.GetContextDB(), guildID, userID)
}

// TriviaUserExists checks if the TriviaUser row exists.
func TriviaUserExists(ctx context.Context, exec boil.ContextExecutor, guildID int64, userID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"trivia_users\" where \"guild_id\"=$1 AND \"user_id\"=$2 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, guildID, userID)
	}
	row := exec.QueryRowContext(ctx, sql, guildID, userID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if trivia_users exists")
	}

	return exists, nil
}

// Exists checks if the TriviaUser row exists.
func (o *TriviaUser) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TriviaUserExists(ctx, exec, o.GuildID, o.UserID)
}
