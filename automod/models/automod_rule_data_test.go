// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testAutomodRuleData(t *testing.T) {
	t.Parallel()

	query := AutomodRuleData()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testAutomodRuleDataDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAutomodRuleDataQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := AutomodRuleData().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAutomodRuleDataSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := AutomodRuleDatumSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAutomodRuleDataExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := AutomodRuleDatumExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if AutomodRuleDatum exists: %s", err)
	}
	if !e {
		t.Errorf("Expected AutomodRuleDatumExists to return true, but got false.")
	}
}

func testAutomodRuleDataFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	automodRuleDatumFound, err := FindAutomodRuleDatum(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if automodRuleDatumFound == nil {
		t.Error("want a record, got nil")
	}
}

func testAutomodRuleDataBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = AutomodRuleData().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testAutomodRuleDataOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := AutomodRuleData().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testAutomodRuleDataAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	automodRuleDatumOne := &AutomodRuleDatum{}
	automodRuleDatumTwo := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, automodRuleDatumOne, automodRuleDatumDBTypes, false, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}
	if err = randomize.Struct(seed, automodRuleDatumTwo, automodRuleDatumDBTypes, false, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = automodRuleDatumOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = automodRuleDatumTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := AutomodRuleData().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testAutomodRuleDataCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	automodRuleDatumOne := &AutomodRuleDatum{}
	automodRuleDatumTwo := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, automodRuleDatumOne, automodRuleDatumDBTypes, false, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}
	if err = randomize.Struct(seed, automodRuleDatumTwo, automodRuleDatumDBTypes, false, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = automodRuleDatumOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = automodRuleDatumTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testAutomodRuleDataInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAutomodRuleDataInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(automodRuleDatumColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAutomodRuleDatumToManyTriggerAutomodTriggeredRules(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a AutomodRuleDatum
	var b, c AutomodTriggeredRule

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, automodTriggeredRuleDBTypes, false, automodTriggeredRuleColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, automodTriggeredRuleDBTypes, false, automodTriggeredRuleColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.TriggerID, a.ID)
	queries.Assign(&c.TriggerID, a.ID)
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.TriggerAutomodTriggeredRules().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.TriggerID, b.TriggerID) {
			bFound = true
		}
		if queries.Equal(v.TriggerID, c.TriggerID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := AutomodRuleDatumSlice{&a}
	if err = a.L.LoadTriggerAutomodTriggeredRules(ctx, tx, false, (*[]*AutomodRuleDatum)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.TriggerAutomodTriggeredRules); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.TriggerAutomodTriggeredRules = nil
	if err = a.L.LoadTriggerAutomodTriggeredRules(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.TriggerAutomodTriggeredRules); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testAutomodRuleDatumToManyAddOpTriggerAutomodTriggeredRules(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a AutomodRuleDatum
	var b, c, d, e AutomodTriggeredRule

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, automodRuleDatumDBTypes, false, strmangle.SetComplement(automodRuleDatumPrimaryKeyColumns, automodRuleDatumColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*AutomodTriggeredRule{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, automodTriggeredRuleDBTypes, false, strmangle.SetComplement(automodTriggeredRulePrimaryKeyColumns, automodTriggeredRuleColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*AutomodTriggeredRule{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddTriggerAutomodTriggeredRules(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ID, first.TriggerID) {
			t.Error("foreign key was wrong value", a.ID, first.TriggerID)
		}
		if !queries.Equal(a.ID, second.TriggerID) {
			t.Error("foreign key was wrong value", a.ID, second.TriggerID)
		}

		if first.R.Trigger != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Trigger != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.TriggerAutomodTriggeredRules[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.TriggerAutomodTriggeredRules[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.TriggerAutomodTriggeredRules().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testAutomodRuleDatumToManySetOpTriggerAutomodTriggeredRules(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a AutomodRuleDatum
	var b, c, d, e AutomodTriggeredRule

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, automodRuleDatumDBTypes, false, strmangle.SetComplement(automodRuleDatumPrimaryKeyColumns, automodRuleDatumColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*AutomodTriggeredRule{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, automodTriggeredRuleDBTypes, false, strmangle.SetComplement(automodTriggeredRulePrimaryKeyColumns, automodTriggeredRuleColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetTriggerAutomodTriggeredRules(ctx, tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.TriggerAutomodTriggeredRules().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetTriggerAutomodTriggeredRules(ctx, tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.TriggerAutomodTriggeredRules().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.TriggerID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.TriggerID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ID, d.TriggerID) {
		t.Error("foreign key was wrong value", a.ID, d.TriggerID)
	}
	if !queries.Equal(a.ID, e.TriggerID) {
		t.Error("foreign key was wrong value", a.ID, e.TriggerID)
	}

	if b.R.Trigger != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Trigger != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Trigger != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Trigger != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.TriggerAutomodTriggeredRules[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.TriggerAutomodTriggeredRules[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testAutomodRuleDatumToManyRemoveOpTriggerAutomodTriggeredRules(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a AutomodRuleDatum
	var b, c, d, e AutomodTriggeredRule

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, automodRuleDatumDBTypes, false, strmangle.SetComplement(automodRuleDatumPrimaryKeyColumns, automodRuleDatumColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*AutomodTriggeredRule{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, automodTriggeredRuleDBTypes, false, strmangle.SetComplement(automodTriggeredRulePrimaryKeyColumns, automodTriggeredRuleColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddTriggerAutomodTriggeredRules(ctx, tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.TriggerAutomodTriggeredRules().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveTriggerAutomodTriggeredRules(ctx, tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.TriggerAutomodTriggeredRules().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.TriggerID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.TriggerID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Trigger != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Trigger != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Trigger != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Trigger != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.TriggerAutomodTriggeredRules) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.TriggerAutomodTriggeredRules[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.TriggerAutomodTriggeredRules[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testAutomodRuleDatumToOneAutomodRuleUsingRule(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local AutomodRuleDatum
	var foreign AutomodRule

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, automodRuleDatumDBTypes, false, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, automodRuleDBTypes, false, automodRuleColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRule struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.RuleID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Rule().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := AutomodRuleDatumSlice{&local}
	if err = local.L.LoadRule(ctx, tx, false, (*[]*AutomodRuleDatum)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Rule == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Rule = nil
	if err = local.L.LoadRule(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Rule == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testAutomodRuleDatumToOneSetOpAutomodRuleUsingRule(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a AutomodRuleDatum
	var b, c AutomodRule

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, automodRuleDatumDBTypes, false, strmangle.SetComplement(automodRuleDatumPrimaryKeyColumns, automodRuleDatumColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, automodRuleDBTypes, false, strmangle.SetComplement(automodRulePrimaryKeyColumns, automodRuleColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, automodRuleDBTypes, false, strmangle.SetComplement(automodRulePrimaryKeyColumns, automodRuleColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*AutomodRule{&b, &c} {
		err = a.SetRule(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Rule != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.RuleAutomodRuleData[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.RuleID != x.ID {
			t.Error("foreign key was wrong value", a.RuleID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.RuleID))
		reflect.Indirect(reflect.ValueOf(&a.RuleID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.RuleID != x.ID {
			t.Error("foreign key was wrong value", a.RuleID, x.ID)
		}
	}
}

func testAutomodRuleDataReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testAutomodRuleDataReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := AutomodRuleDatumSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testAutomodRuleDataSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := AutomodRuleData().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	automodRuleDatumDBTypes = map[string]string{`ID`: `bigint`, `GuildID`: `bigint`, `RuleID`: `bigint`, `Kind`: `integer`, `TypeID`: `integer`, `Settings`: `jsonb`}
	_                       = bytes.MinRead
)

func testAutomodRuleDataUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(automodRuleDatumPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(automodRuleDatumColumns) == len(automodRuleDatumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testAutomodRuleDataSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(automodRuleDatumColumns) == len(automodRuleDatumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &AutomodRuleDatum{}
	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, automodRuleDatumDBTypes, true, automodRuleDatumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(automodRuleDatumColumns, automodRuleDatumPrimaryKeyColumns) {
		fields = automodRuleDatumColumns
	} else {
		fields = strmangle.SetComplement(
			automodRuleDatumColumns,
			automodRuleDatumPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := AutomodRuleDatumSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testAutomodRuleDataUpsert(t *testing.T) {
	t.Parallel()

	if len(automodRuleDatumColumns) == len(automodRuleDatumPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := AutomodRuleDatum{}
	if err = randomize.Struct(seed, &o, automodRuleDatumDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert AutomodRuleDatum: %s", err)
	}

	count, err := AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, automodRuleDatumDBTypes, false, automodRuleDatumPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize AutomodRuleDatum struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert AutomodRuleDatum: %s", err)
	}

	count, err = AutomodRuleData().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
