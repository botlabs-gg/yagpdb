// Code generated by SQLBoiler 4.14.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// PersonalizedGuild is an object representing the database table.
type PersonalizedGuild struct {
	GuildID   int64       `boil:"guild_id" json:"guild_id" toml:"guild_id" yaml:"guild_id"`
	Nick      null.String `boil:"nick" json:"nick,omitempty" toml:"nick" yaml:"nick,omitempty"`
	Avatar    null.String `boil:"avatar" json:"avatar,omitempty" toml:"avatar" yaml:"avatar,omitempty"`
	Banner    null.String `boil:"banner" json:"banner,omitempty" toml:"banner" yaml:"banner,omitempty"`
	UpdatedAt time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *personalizedGuildR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L personalizedGuildL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PersonalizedGuildColumns = struct {
	GuildID   string
	Nick      string
	Avatar    string
	Banner    string
	UpdatedAt string
}{
	GuildID:   "guild_id",
	Nick:      "nick",
	Avatar:    "avatar",
	Banner:    "banner",
	UpdatedAt: "updated_at",
}

var PersonalizedGuildTableColumns = struct {
	GuildID   string
	Nick      string
	Avatar    string
	Banner    string
	UpdatedAt string
}{
	GuildID:   "personalized_guilds.guild_id",
	Nick:      "personalized_guilds.nick",
	Avatar:    "personalized_guilds.avatar",
	Banner:    "personalized_guilds.banner",
	UpdatedAt: "personalized_guilds.updated_at",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelpernull_String struct{ field string }

func (w whereHelpernull_String) EQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_String) NEQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_String) LT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_String) LTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_String) GT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_String) GTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_String) IN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_String) NIN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_String) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_String) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var PersonalizedGuildWhere = struct {
	GuildID   whereHelperint64
	Nick      whereHelpernull_String
	Avatar    whereHelpernull_String
	Banner    whereHelpernull_String
	UpdatedAt whereHelpertime_Time
}{
	GuildID:   whereHelperint64{field: "\"personalized_guilds\".\"guild_id\""},
	Nick:      whereHelpernull_String{field: "\"personalized_guilds\".\"nick\""},
	Avatar:    whereHelpernull_String{field: "\"personalized_guilds\".\"avatar\""},
	Banner:    whereHelpernull_String{field: "\"personalized_guilds\".\"banner\""},
	UpdatedAt: whereHelpertime_Time{field: "\"personalized_guilds\".\"updated_at\""},
}

// PersonalizedGuildRels is where relationship names are stored.
var PersonalizedGuildRels = struct {
}{}

// personalizedGuildR is where relationships are stored.
type personalizedGuildR struct {
}

// NewStruct creates a new relationship struct
func (*personalizedGuildR) NewStruct() *personalizedGuildR {
	return &personalizedGuildR{}
}

// personalizedGuildL is where Load methods for each relationship are stored.
type personalizedGuildL struct{}

var (
	personalizedGuildAllColumns            = []string{"guild_id", "nick", "avatar", "banner", "updated_at"}
	personalizedGuildColumnsWithoutDefault = []string{"guild_id"}
	personalizedGuildColumnsWithDefault    = []string{"nick", "avatar", "banner", "updated_at"}
	personalizedGuildPrimaryKeyColumns     = []string{"guild_id"}
	personalizedGuildGeneratedColumns      = []string{}
)

type (
	// PersonalizedGuildSlice is an alias for a slice of pointers to PersonalizedGuild.
	// This should almost always be used instead of []PersonalizedGuild.
	PersonalizedGuildSlice []*PersonalizedGuild

	personalizedGuildQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	personalizedGuildType                 = reflect.TypeOf(&PersonalizedGuild{})
	personalizedGuildMapping              = queries.MakeStructMapping(personalizedGuildType)
	personalizedGuildPrimaryKeyMapping, _ = queries.BindMapping(personalizedGuildType, personalizedGuildMapping, personalizedGuildPrimaryKeyColumns)
	personalizedGuildInsertCacheMut       sync.RWMutex
	personalizedGuildInsertCache          = make(map[string]insertCache)
	personalizedGuildUpdateCacheMut       sync.RWMutex
	personalizedGuildUpdateCache          = make(map[string]updateCache)
	personalizedGuildUpsertCacheMut       sync.RWMutex
	personalizedGuildUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single personalizedGuild record from the query using the global executor.
func (q personalizedGuildQuery) OneG(ctx context.Context) (*PersonalizedGuild, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single personalizedGuild record from the query.
func (q personalizedGuildQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PersonalizedGuild, error) {
	o := &PersonalizedGuild{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for personalized_guilds")
	}

	return o, nil
}

// AllG returns all PersonalizedGuild records from the query using the global executor.
func (q personalizedGuildQuery) AllG(ctx context.Context) (PersonalizedGuildSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all PersonalizedGuild records from the query.
func (q personalizedGuildQuery) All(ctx context.Context, exec boil.ContextExecutor) (PersonalizedGuildSlice, error) {
	var o []*PersonalizedGuild

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PersonalizedGuild slice")
	}

	return o, nil
}

// CountG returns the count of all PersonalizedGuild records in the query using the global executor
func (q personalizedGuildQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all PersonalizedGuild records in the query.
func (q personalizedGuildQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count personalized_guilds rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q personalizedGuildQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q personalizedGuildQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if personalized_guilds exists")
	}

	return count > 0, nil
}

// PersonalizedGuilds retrieves all the records using an executor.
func PersonalizedGuilds(mods ...qm.QueryMod) personalizedGuildQuery {
	mods = append(mods, qm.From("\"personalized_guilds\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"personalized_guilds\".*"})
	}

	return personalizedGuildQuery{q}
}

// FindPersonalizedGuildG retrieves a single record by ID.
func FindPersonalizedGuildG(ctx context.Context, guildID int64, selectCols ...string) (*PersonalizedGuild, error) {
	return FindPersonalizedGuild(ctx, boil.GetContextDB(), guildID, selectCols...)
}

// FindPersonalizedGuild retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPersonalizedGuild(ctx context.Context, exec boil.ContextExecutor, guildID int64, selectCols ...string) (*PersonalizedGuild, error) {
	personalizedGuildObj := &PersonalizedGuild{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"personalized_guilds\" where \"guild_id\"=$1", sel,
	)

	q := queries.Raw(query, guildID)

	err := q.Bind(ctx, exec, personalizedGuildObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from personalized_guilds")
	}

	return personalizedGuildObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *PersonalizedGuild) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PersonalizedGuild) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no personalized_guilds provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(personalizedGuildColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	personalizedGuildInsertCacheMut.RLock()
	cache, cached := personalizedGuildInsertCache[key]
	personalizedGuildInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			personalizedGuildAllColumns,
			personalizedGuildColumnsWithDefault,
			personalizedGuildColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(personalizedGuildType, personalizedGuildMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(personalizedGuildType, personalizedGuildMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"personalized_guilds\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"personalized_guilds\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into personalized_guilds")
	}

	if !cached {
		personalizedGuildInsertCacheMut.Lock()
		personalizedGuildInsertCache[key] = cache
		personalizedGuildInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single PersonalizedGuild record using the global executor.
// See Update for more documentation.
func (o *PersonalizedGuild) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the PersonalizedGuild.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PersonalizedGuild) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	key := makeCacheKey(columns, nil)
	personalizedGuildUpdateCacheMut.RLock()
	cache, cached := personalizedGuildUpdateCache[key]
	personalizedGuildUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			personalizedGuildAllColumns,
			personalizedGuildPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update personalized_guilds, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"personalized_guilds\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, personalizedGuildPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(personalizedGuildType, personalizedGuildMapping, append(wl, personalizedGuildPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update personalized_guilds row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for personalized_guilds")
	}

	if !cached {
		personalizedGuildUpdateCacheMut.Lock()
		personalizedGuildUpdateCache[key] = cache
		personalizedGuildUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q personalizedGuildQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q personalizedGuildQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for personalized_guilds")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for personalized_guilds")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o PersonalizedGuildSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PersonalizedGuildSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), personalizedGuildPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"personalized_guilds\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, personalizedGuildPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in personalizedGuild slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all personalizedGuild")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *PersonalizedGuild) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PersonalizedGuild) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no personalized_guilds provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	nzDefaults := queries.NonZeroDefaultSet(personalizedGuildColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	personalizedGuildUpsertCacheMut.RLock()
	cache, cached := personalizedGuildUpsertCache[key]
	personalizedGuildUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			personalizedGuildAllColumns,
			personalizedGuildColumnsWithDefault,
			personalizedGuildColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			personalizedGuildAllColumns,
			personalizedGuildPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert personalized_guilds, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(personalizedGuildPrimaryKeyColumns))
			copy(conflict, personalizedGuildPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"personalized_guilds\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(personalizedGuildType, personalizedGuildMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(personalizedGuildType, personalizedGuildMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert personalized_guilds")
	}

	if !cached {
		personalizedGuildUpsertCacheMut.Lock()
		personalizedGuildUpsertCache[key] = cache
		personalizedGuildUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single PersonalizedGuild record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *PersonalizedGuild) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single PersonalizedGuild record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PersonalizedGuild) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PersonalizedGuild provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), personalizedGuildPrimaryKeyMapping)
	sql := "DELETE FROM \"personalized_guilds\" WHERE \"guild_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from personalized_guilds")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for personalized_guilds")
	}

	return rowsAff, nil
}

func (q personalizedGuildQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q personalizedGuildQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no personalizedGuildQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from personalized_guilds")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for personalized_guilds")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o PersonalizedGuildSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PersonalizedGuildSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), personalizedGuildPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"personalized_guilds\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, personalizedGuildPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from personalizedGuild slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for personalized_guilds")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *PersonalizedGuild) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no PersonalizedGuild provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PersonalizedGuild) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPersonalizedGuild(ctx, exec, o.GuildID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PersonalizedGuildSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty PersonalizedGuildSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PersonalizedGuildSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PersonalizedGuildSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), personalizedGuildPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"personalized_guilds\".* FROM \"personalized_guilds\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, personalizedGuildPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PersonalizedGuildSlice")
	}

	*o = slice

	return nil
}

// PersonalizedGuildExistsG checks if the PersonalizedGuild row exists.
func PersonalizedGuildExistsG(ctx context.Context, guildID int64) (bool, error) {
	return PersonalizedGuildExists(ctx, boil.GetContextDB(), guildID)
}

// PersonalizedGuildExists checks if the PersonalizedGuild row exists.
func PersonalizedGuildExists(ctx context.Context, exec boil.ContextExecutor, guildID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"personalized_guilds\" where \"guild_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, guildID)
	}
	row := exec.QueryRowContext(ctx, sql, guildID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if personalized_guilds exists")
	}

	return exists, nil
}

// Exists checks if the PersonalizedGuild row exists.
func (o *PersonalizedGuild) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PersonalizedGuildExists(ctx, exec, o.GuildID)
}
