// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSoundboardSounds(t *testing.T) {
	t.Parallel()

	query := SoundboardSounds()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSoundboardSoundsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSoundboardSoundsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := SoundboardSounds().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSoundboardSoundsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SoundboardSoundSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSoundboardSoundsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SoundboardSoundExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if SoundboardSound exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SoundboardSoundExists to return true, but got false.")
	}
}

func testSoundboardSoundsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	soundboardSoundFound, err := FindSoundboardSound(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if soundboardSoundFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSoundboardSoundsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = SoundboardSounds().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testSoundboardSoundsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := SoundboardSounds().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSoundboardSoundsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	soundboardSoundOne := &SoundboardSound{}
	soundboardSoundTwo := &SoundboardSound{}
	if err = randomize.Struct(seed, soundboardSoundOne, soundboardSoundDBTypes, false, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}
	if err = randomize.Struct(seed, soundboardSoundTwo, soundboardSoundDBTypes, false, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = soundboardSoundOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = soundboardSoundTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SoundboardSounds().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSoundboardSoundsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	soundboardSoundOne := &SoundboardSound{}
	soundboardSoundTwo := &SoundboardSound{}
	if err = randomize.Struct(seed, soundboardSoundOne, soundboardSoundDBTypes, false, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}
	if err = randomize.Struct(seed, soundboardSoundTwo, soundboardSoundDBTypes, false, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = soundboardSoundOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = soundboardSoundTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testSoundboardSoundsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSoundboardSoundsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(soundboardSoundColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSoundboardSoundsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSoundboardSoundsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SoundboardSoundSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSoundboardSoundsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SoundboardSounds().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	soundboardSoundDBTypes = map[string]string{`BlacklistedRoles`: `ARRAYbigint`, `CreatedAt`: `timestamp with time zone`, `GuildID`: `bigint`, `ID`: `integer`, `Name`: `text`, `RequiredRole`: `text`, `RequiredRoles`: `ARRAYbigint`, `Status`: `integer`, `UpdatedAt`: `timestamp with time zone`}
	_                      = bytes.MinRead
)

func testSoundboardSoundsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(soundboardSoundPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(soundboardSoundColumns) == len(soundboardSoundPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSoundboardSoundsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(soundboardSoundColumns) == len(soundboardSoundPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SoundboardSound{}
	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, soundboardSoundDBTypes, true, soundboardSoundPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(soundboardSoundColumns, soundboardSoundPrimaryKeyColumns) {
		fields = soundboardSoundColumns
	} else {
		fields = strmangle.SetComplement(
			soundboardSoundColumns,
			soundboardSoundPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SoundboardSoundSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSoundboardSoundsUpsert(t *testing.T) {
	t.Parallel()

	if len(soundboardSoundColumns) == len(soundboardSoundPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := SoundboardSound{}
	if err = randomize.Struct(seed, &o, soundboardSoundDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SoundboardSound: %s", err)
	}

	count, err := SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, soundboardSoundDBTypes, false, soundboardSoundPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SoundboardSound struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SoundboardSound: %s", err)
	}

	count, err = SoundboardSounds().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
