// Code generated by SQLBoiler 4.19.5 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// TwitchChannelSubscription is an object representing the database table.
type TwitchChannelSubscription struct {
	ID              int              `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt       time.Time        `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time        `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	GuildID         string           `boil:"guild_id" json:"guild_id" toml:"guild_id" yaml:"guild_id"`
	ChannelID       string           `boil:"channel_id" json:"channel_id" toml:"channel_id" yaml:"channel_id"`
	TwitchUserID    string           `boil:"twitch_user_id" json:"twitch_user_id" toml:"twitch_user_id" yaml:"twitch_user_id"`
	TwitchUsername  string           `boil:"twitch_username" json:"twitch_username" toml:"twitch_username" yaml:"twitch_username"`
	MentionEveryone bool             `boil:"mention_everyone" json:"mention_everyone" toml:"mention_everyone" yaml:"mention_everyone"`
	MentionRoles    types.Int64Array `boil:"mention_roles" json:"mention_roles,omitempty" toml:"mention_roles" yaml:"mention_roles,omitempty"`
	PublishVod      bool             `boil:"publish_vod" json:"publish_vod" toml:"publish_vod" yaml:"publish_vod"`
	Enabled         bool             `boil:"enabled" json:"enabled" toml:"enabled" yaml:"enabled"`

	R *twitchChannelSubscriptionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L twitchChannelSubscriptionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TwitchChannelSubscriptionColumns = struct {
	ID              string
	CreatedAt       string
	UpdatedAt       string
	GuildID         string
	ChannelID       string
	TwitchUserID    string
	TwitchUsername  string
	MentionEveryone string
	MentionRoles    string
	PublishVod      string
	Enabled         string
}{
	ID:              "id",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
	GuildID:         "guild_id",
	ChannelID:       "channel_id",
	TwitchUserID:    "twitch_user_id",
	TwitchUsername:  "twitch_username",
	MentionEveryone: "mention_everyone",
	MentionRoles:    "mention_roles",
	PublishVod:      "publish_vod",
	Enabled:         "enabled",
}

var TwitchChannelSubscriptionTableColumns = struct {
	ID              string
	CreatedAt       string
	UpdatedAt       string
	GuildID         string
	ChannelID       string
	TwitchUserID    string
	TwitchUsername  string
	MentionEveryone string
	MentionRoles    string
	PublishVod      string
	Enabled         string
}{
	ID:              "twitch_channel_subscriptions.id",
	CreatedAt:       "twitch_channel_subscriptions.created_at",
	UpdatedAt:       "twitch_channel_subscriptions.updated_at",
	GuildID:         "twitch_channel_subscriptions.guild_id",
	ChannelID:       "twitch_channel_subscriptions.channel_id",
	TwitchUserID:    "twitch_channel_subscriptions.twitch_user_id",
	TwitchUsername:  "twitch_channel_subscriptions.twitch_username",
	MentionEveryone: "twitch_channel_subscriptions.mention_everyone",
	MentionRoles:    "twitch_channel_subscriptions.mention_roles",
	PublishVod:      "twitch_channel_subscriptions.publish_vod",
	Enabled:         "twitch_channel_subscriptions.enabled",
}

// Generated where

type whereHelperint struct{ field string }

func (w whereHelperint) EQ(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint) NEQ(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint) LT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint) LTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint) GT(x int) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint) GTE(x int) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint) IN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint) NIN(slice []int) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

type whereHelpertypes_Int64Array struct{ field string }

func (w whereHelpertypes_Int64Array) EQ(x types.Int64Array) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpertypes_Int64Array) NEQ(x types.Int64Array) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpertypes_Int64Array) LT(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_Int64Array) LTE(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_Int64Array) GT(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_Int64Array) GTE(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpertypes_Int64Array) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpertypes_Int64Array) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var TwitchChannelSubscriptionWhere = struct {
	ID              whereHelperint
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
	GuildID         whereHelperstring
	ChannelID       whereHelperstring
	TwitchUserID    whereHelperstring
	TwitchUsername  whereHelperstring
	MentionEveryone whereHelperbool
	MentionRoles    whereHelpertypes_Int64Array
	PublishVod      whereHelperbool
	Enabled         whereHelperbool
}{
	ID:              whereHelperint{field: "\"twitch_channel_subscriptions\".\"id\""},
	CreatedAt:       whereHelpertime_Time{field: "\"twitch_channel_subscriptions\".\"created_at\""},
	UpdatedAt:       whereHelpertime_Time{field: "\"twitch_channel_subscriptions\".\"updated_at\""},
	GuildID:         whereHelperstring{field: "\"twitch_channel_subscriptions\".\"guild_id\""},
	ChannelID:       whereHelperstring{field: "\"twitch_channel_subscriptions\".\"channel_id\""},
	TwitchUserID:    whereHelperstring{field: "\"twitch_channel_subscriptions\".\"twitch_user_id\""},
	TwitchUsername:  whereHelperstring{field: "\"twitch_channel_subscriptions\".\"twitch_username\""},
	MentionEveryone: whereHelperbool{field: "\"twitch_channel_subscriptions\".\"mention_everyone\""},
	MentionRoles:    whereHelpertypes_Int64Array{field: "\"twitch_channel_subscriptions\".\"mention_roles\""},
	PublishVod:      whereHelperbool{field: "\"twitch_channel_subscriptions\".\"publish_vod\""},
	Enabled:         whereHelperbool{field: "\"twitch_channel_subscriptions\".\"enabled\""},
}

// TwitchChannelSubscriptionRels is where relationship names are stored.
var TwitchChannelSubscriptionRels = struct {
}{}

// twitchChannelSubscriptionR is where relationships are stored.
type twitchChannelSubscriptionR struct {
}

// NewStruct creates a new relationship struct
func (*twitchChannelSubscriptionR) NewStruct() *twitchChannelSubscriptionR {
	return &twitchChannelSubscriptionR{}
}

// twitchChannelSubscriptionL is where Load methods for each relationship are stored.
type twitchChannelSubscriptionL struct{}

var (
	twitchChannelSubscriptionAllColumns            = []string{"id", "created_at", "updated_at", "guild_id", "channel_id", "twitch_user_id", "twitch_username", "mention_everyone", "mention_roles", "publish_vod", "enabled"}
	twitchChannelSubscriptionColumnsWithoutDefault = []string{"created_at", "updated_at", "guild_id", "channel_id", "twitch_user_id", "twitch_username", "mention_everyone"}
	twitchChannelSubscriptionColumnsWithDefault    = []string{"id", "mention_roles", "publish_vod", "enabled"}
	twitchChannelSubscriptionPrimaryKeyColumns     = []string{"id"}
	twitchChannelSubscriptionGeneratedColumns      = []string{}
)

type (
	// TwitchChannelSubscriptionSlice is an alias for a slice of pointers to TwitchChannelSubscription.
	// This should almost always be used instead of []TwitchChannelSubscription.
	TwitchChannelSubscriptionSlice []*TwitchChannelSubscription

	twitchChannelSubscriptionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	twitchChannelSubscriptionType                 = reflect.TypeOf(&TwitchChannelSubscription{})
	twitchChannelSubscriptionMapping              = queries.MakeStructMapping(twitchChannelSubscriptionType)
	twitchChannelSubscriptionPrimaryKeyMapping, _ = queries.BindMapping(twitchChannelSubscriptionType, twitchChannelSubscriptionMapping, twitchChannelSubscriptionPrimaryKeyColumns)
	twitchChannelSubscriptionInsertCacheMut       sync.RWMutex
	twitchChannelSubscriptionInsertCache          = make(map[string]insertCache)
	twitchChannelSubscriptionUpdateCacheMut       sync.RWMutex
	twitchChannelSubscriptionUpdateCache          = make(map[string]updateCache)
	twitchChannelSubscriptionUpsertCacheMut       sync.RWMutex
	twitchChannelSubscriptionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single twitchChannelSubscription record from the query using the global executor.
func (q twitchChannelSubscriptionQuery) OneG(ctx context.Context) (*TwitchChannelSubscription, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single twitchChannelSubscription record from the query.
func (q twitchChannelSubscriptionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TwitchChannelSubscription, error) {
	o := &TwitchChannelSubscription{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for twitch_channel_subscriptions")
	}

	return o, nil
}

// AllG returns all TwitchChannelSubscription records from the query using the global executor.
func (q twitchChannelSubscriptionQuery) AllG(ctx context.Context) (TwitchChannelSubscriptionSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all TwitchChannelSubscription records from the query.
func (q twitchChannelSubscriptionQuery) All(ctx context.Context, exec boil.ContextExecutor) (TwitchChannelSubscriptionSlice, error) {
	var o []*TwitchChannelSubscription

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TwitchChannelSubscription slice")
	}

	return o, nil
}

// CountG returns the count of all TwitchChannelSubscription records in the query using the global executor
func (q twitchChannelSubscriptionQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all TwitchChannelSubscription records in the query.
func (q twitchChannelSubscriptionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count twitch_channel_subscriptions rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q twitchChannelSubscriptionQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q twitchChannelSubscriptionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if twitch_channel_subscriptions exists")
	}

	return count > 0, nil
}

// TwitchChannelSubscriptions retrieves all the records using an executor.
func TwitchChannelSubscriptions(mods ...qm.QueryMod) twitchChannelSubscriptionQuery {
	mods = append(mods, qm.From("\"twitch_channel_subscriptions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"twitch_channel_subscriptions\".*"})
	}

	return twitchChannelSubscriptionQuery{q}
}

// FindTwitchChannelSubscriptionG retrieves a single record by ID.
func FindTwitchChannelSubscriptionG(ctx context.Context, iD int, selectCols ...string) (*TwitchChannelSubscription, error) {
	return FindTwitchChannelSubscription(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTwitchChannelSubscription retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTwitchChannelSubscription(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*TwitchChannelSubscription, error) {
	twitchChannelSubscriptionObj := &TwitchChannelSubscription{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"twitch_channel_subscriptions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, twitchChannelSubscriptionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from twitch_channel_subscriptions")
	}

	return twitchChannelSubscriptionObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TwitchChannelSubscription) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TwitchChannelSubscription) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no twitch_channel_subscriptions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(twitchChannelSubscriptionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	twitchChannelSubscriptionInsertCacheMut.RLock()
	cache, cached := twitchChannelSubscriptionInsertCache[key]
	twitchChannelSubscriptionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			twitchChannelSubscriptionAllColumns,
			twitchChannelSubscriptionColumnsWithDefault,
			twitchChannelSubscriptionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(twitchChannelSubscriptionType, twitchChannelSubscriptionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(twitchChannelSubscriptionType, twitchChannelSubscriptionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"twitch_channel_subscriptions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"twitch_channel_subscriptions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into twitch_channel_subscriptions")
	}

	if !cached {
		twitchChannelSubscriptionInsertCacheMut.Lock()
		twitchChannelSubscriptionInsertCache[key] = cache
		twitchChannelSubscriptionInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single TwitchChannelSubscription record using the global executor.
// See Update for more documentation.
func (o *TwitchChannelSubscription) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the TwitchChannelSubscription.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TwitchChannelSubscription) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	key := makeCacheKey(columns, nil)
	twitchChannelSubscriptionUpdateCacheMut.RLock()
	cache, cached := twitchChannelSubscriptionUpdateCache[key]
	twitchChannelSubscriptionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			twitchChannelSubscriptionAllColumns,
			twitchChannelSubscriptionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update twitch_channel_subscriptions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"twitch_channel_subscriptions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, twitchChannelSubscriptionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(twitchChannelSubscriptionType, twitchChannelSubscriptionMapping, append(wl, twitchChannelSubscriptionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update twitch_channel_subscriptions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for twitch_channel_subscriptions")
	}

	if !cached {
		twitchChannelSubscriptionUpdateCacheMut.Lock()
		twitchChannelSubscriptionUpdateCache[key] = cache
		twitchChannelSubscriptionUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q twitchChannelSubscriptionQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q twitchChannelSubscriptionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for twitch_channel_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for twitch_channel_subscriptions")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TwitchChannelSubscriptionSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TwitchChannelSubscriptionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), twitchChannelSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"twitch_channel_subscriptions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, twitchChannelSubscriptionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in twitchChannelSubscription slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all twitchChannelSubscription")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TwitchChannelSubscription) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TwitchChannelSubscription) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no twitch_channel_subscriptions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	nzDefaults := queries.NonZeroDefaultSet(twitchChannelSubscriptionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	twitchChannelSubscriptionUpsertCacheMut.RLock()
	cache, cached := twitchChannelSubscriptionUpsertCache[key]
	twitchChannelSubscriptionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			twitchChannelSubscriptionAllColumns,
			twitchChannelSubscriptionColumnsWithDefault,
			twitchChannelSubscriptionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			twitchChannelSubscriptionAllColumns,
			twitchChannelSubscriptionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert twitch_channel_subscriptions, could not build update column list")
		}

		ret := strmangle.SetComplement(twitchChannelSubscriptionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(twitchChannelSubscriptionPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert twitch_channel_subscriptions, could not build conflict column list")
			}

			conflict = make([]string, len(twitchChannelSubscriptionPrimaryKeyColumns))
			copy(conflict, twitchChannelSubscriptionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"twitch_channel_subscriptions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(twitchChannelSubscriptionType, twitchChannelSubscriptionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(twitchChannelSubscriptionType, twitchChannelSubscriptionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert twitch_channel_subscriptions")
	}

	if !cached {
		twitchChannelSubscriptionUpsertCacheMut.Lock()
		twitchChannelSubscriptionUpsertCache[key] = cache
		twitchChannelSubscriptionUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single TwitchChannelSubscription record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TwitchChannelSubscription) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single TwitchChannelSubscription record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TwitchChannelSubscription) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TwitchChannelSubscription provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), twitchChannelSubscriptionPrimaryKeyMapping)
	sql := "DELETE FROM \"twitch_channel_subscriptions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from twitch_channel_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for twitch_channel_subscriptions")
	}

	return rowsAff, nil
}

func (q twitchChannelSubscriptionQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q twitchChannelSubscriptionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no twitchChannelSubscriptionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from twitch_channel_subscriptions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for twitch_channel_subscriptions")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TwitchChannelSubscriptionSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TwitchChannelSubscriptionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), twitchChannelSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"twitch_channel_subscriptions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, twitchChannelSubscriptionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from twitchChannelSubscription slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for twitch_channel_subscriptions")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TwitchChannelSubscription) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no TwitchChannelSubscription provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TwitchChannelSubscription) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTwitchChannelSubscription(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TwitchChannelSubscriptionSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty TwitchChannelSubscriptionSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TwitchChannelSubscriptionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TwitchChannelSubscriptionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), twitchChannelSubscriptionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"twitch_channel_subscriptions\".* FROM \"twitch_channel_subscriptions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, twitchChannelSubscriptionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TwitchChannelSubscriptionSlice")
	}

	*o = slice

	return nil
}

// TwitchChannelSubscriptionExistsG checks if the TwitchChannelSubscription row exists.
func TwitchChannelSubscriptionExistsG(ctx context.Context, iD int) (bool, error) {
	return TwitchChannelSubscriptionExists(ctx, boil.GetContextDB(), iD)
}

// TwitchChannelSubscriptionExists checks if the TwitchChannelSubscription row exists.
func TwitchChannelSubscriptionExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"twitch_channel_subscriptions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if twitch_channel_subscriptions exists")
	}

	return exists, nil
}

// Exists checks if the TwitchChannelSubscription row exists.
func (o *TwitchChannelSubscription) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TwitchChannelSubscriptionExists(ctx, exec, o.ID)
}
