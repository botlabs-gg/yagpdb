// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testCustomCommandGroups(t *testing.T) {
	t.Parallel()

	query := CustomCommandGroups()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testCustomCommandGroupsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCustomCommandGroupsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := CustomCommandGroups().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCustomCommandGroupsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CustomCommandGroupSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testCustomCommandGroupsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := CustomCommandGroupExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if CustomCommandGroup exists: %s", err)
	}
	if !e {
		t.Errorf("Expected CustomCommandGroupExists to return true, but got false.")
	}
}

func testCustomCommandGroupsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	customCommandGroupFound, err := FindCustomCommandGroup(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if customCommandGroupFound == nil {
		t.Error("want a record, got nil")
	}
}

func testCustomCommandGroupsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = CustomCommandGroups().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testCustomCommandGroupsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := CustomCommandGroups().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testCustomCommandGroupsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	customCommandGroupOne := &CustomCommandGroup{}
	customCommandGroupTwo := &CustomCommandGroup{}
	if err = randomize.Struct(seed, customCommandGroupOne, customCommandGroupDBTypes, false, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}
	if err = randomize.Struct(seed, customCommandGroupTwo, customCommandGroupDBTypes, false, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = customCommandGroupOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = customCommandGroupTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := CustomCommandGroups().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testCustomCommandGroupsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	customCommandGroupOne := &CustomCommandGroup{}
	customCommandGroupTwo := &CustomCommandGroup{}
	if err = randomize.Struct(seed, customCommandGroupOne, customCommandGroupDBTypes, false, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}
	if err = randomize.Struct(seed, customCommandGroupTwo, customCommandGroupDBTypes, false, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = customCommandGroupOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = customCommandGroupTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testCustomCommandGroupsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCustomCommandGroupsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(customCommandGroupColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testCustomCommandGroupToManyGroupCustomCommands(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a CustomCommandGroup
	var b, c CustomCommand

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, customCommandDBTypes, false, customCommandColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, customCommandDBTypes, false, customCommandColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.GroupID, a.ID)
	queries.Assign(&c.GroupID, a.ID)
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.GroupCustomCommands().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.GroupID, b.GroupID) {
			bFound = true
		}
		if queries.Equal(v.GroupID, c.GroupID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := CustomCommandGroupSlice{&a}
	if err = a.L.LoadGroupCustomCommands(ctx, tx, false, (*[]*CustomCommandGroup)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.GroupCustomCommands); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.GroupCustomCommands = nil
	if err = a.L.LoadGroupCustomCommands(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.GroupCustomCommands); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testCustomCommandGroupToManyAddOpGroupCustomCommands(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a CustomCommandGroup
	var b, c, d, e CustomCommand

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, customCommandGroupDBTypes, false, strmangle.SetComplement(customCommandGroupPrimaryKeyColumns, customCommandGroupColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*CustomCommand{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, customCommandDBTypes, false, strmangle.SetComplement(customCommandPrimaryKeyColumns, customCommandColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*CustomCommand{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddGroupCustomCommands(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.ID, first.GroupID) {
			t.Error("foreign key was wrong value", a.ID, first.GroupID)
		}
		if !queries.Equal(a.ID, second.GroupID) {
			t.Error("foreign key was wrong value", a.ID, second.GroupID)
		}

		if first.R.Group != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Group != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.GroupCustomCommands[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.GroupCustomCommands[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.GroupCustomCommands().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testCustomCommandGroupToManySetOpGroupCustomCommands(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a CustomCommandGroup
	var b, c, d, e CustomCommand

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, customCommandGroupDBTypes, false, strmangle.SetComplement(customCommandGroupPrimaryKeyColumns, customCommandGroupColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*CustomCommand{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, customCommandDBTypes, false, strmangle.SetComplement(customCommandPrimaryKeyColumns, customCommandColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetGroupCustomCommands(ctx, tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.GroupCustomCommands().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetGroupCustomCommands(ctx, tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.GroupCustomCommands().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.GroupID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.GroupID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.ID, d.GroupID) {
		t.Error("foreign key was wrong value", a.ID, d.GroupID)
	}
	if !queries.Equal(a.ID, e.GroupID) {
		t.Error("foreign key was wrong value", a.ID, e.GroupID)
	}

	if b.R.Group != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Group != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Group != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Group != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.GroupCustomCommands[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.GroupCustomCommands[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testCustomCommandGroupToManyRemoveOpGroupCustomCommands(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a CustomCommandGroup
	var b, c, d, e CustomCommand

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, customCommandGroupDBTypes, false, strmangle.SetComplement(customCommandGroupPrimaryKeyColumns, customCommandGroupColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*CustomCommand{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, customCommandDBTypes, false, strmangle.SetComplement(customCommandPrimaryKeyColumns, customCommandColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddGroupCustomCommands(ctx, tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.GroupCustomCommands().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveGroupCustomCommands(ctx, tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.GroupCustomCommands().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.GroupID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.GroupID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Group != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Group != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Group != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Group != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.GroupCustomCommands) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.GroupCustomCommands[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.GroupCustomCommands[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testCustomCommandGroupsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testCustomCommandGroupsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := CustomCommandGroupSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testCustomCommandGroupsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := CustomCommandGroups().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	customCommandGroupDBTypes = map[string]string{`ID`: `bigint`, `GuildID`: `bigint`, `Name`: `text`, `IgnoreRoles`: `ARRAYbigint`, `IgnoreChannels`: `ARRAYbigint`, `WhitelistRoles`: `ARRAYbigint`, `WhitelistChannels`: `ARRAYbigint`}
	_                         = bytes.MinRead
)

func testCustomCommandGroupsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(customCommandGroupPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(customCommandGroupColumns) == len(customCommandGroupPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testCustomCommandGroupsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(customCommandGroupColumns) == len(customCommandGroupPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &CustomCommandGroup{}
	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, customCommandGroupDBTypes, true, customCommandGroupPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(customCommandGroupColumns, customCommandGroupPrimaryKeyColumns) {
		fields = customCommandGroupColumns
	} else {
		fields = strmangle.SetComplement(
			customCommandGroupColumns,
			customCommandGroupPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := CustomCommandGroupSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testCustomCommandGroupsUpsert(t *testing.T) {
	t.Parallel()

	if len(customCommandGroupColumns) == len(customCommandGroupPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := CustomCommandGroup{}
	if err = randomize.Struct(seed, &o, customCommandGroupDBTypes, true); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert CustomCommandGroup: %s", err)
	}

	count, err := CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, customCommandGroupDBTypes, false, customCommandGroupPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize CustomCommandGroup struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert CustomCommandGroup: %s", err)
	}

	count, err = CustomCommandGroups().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
