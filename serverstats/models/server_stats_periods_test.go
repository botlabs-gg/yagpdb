// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testServerStatsPeriods(t *testing.T) {
	t.Parallel()

	query := ServerStatsPeriods()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testServerStatsPeriodsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testServerStatsPeriodsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := ServerStatsPeriods().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testServerStatsPeriodsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ServerStatsPeriodSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testServerStatsPeriodsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := ServerStatsPeriodExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if ServerStatsPeriod exists: %s", err)
	}
	if !e {
		t.Errorf("Expected ServerStatsPeriodExists to return true, but got false.")
	}
}

func testServerStatsPeriodsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	serverStatsPeriodFound, err := FindServerStatsPeriod(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if serverStatsPeriodFound == nil {
		t.Error("want a record, got nil")
	}
}

func testServerStatsPeriodsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = ServerStatsPeriods().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testServerStatsPeriodsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := ServerStatsPeriods().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testServerStatsPeriodsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	serverStatsPeriodOne := &ServerStatsPeriod{}
	serverStatsPeriodTwo := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, serverStatsPeriodOne, serverStatsPeriodDBTypes, false, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}
	if err = randomize.Struct(seed, serverStatsPeriodTwo, serverStatsPeriodDBTypes, false, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = serverStatsPeriodOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = serverStatsPeriodTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ServerStatsPeriods().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testServerStatsPeriodsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	serverStatsPeriodOne := &ServerStatsPeriod{}
	serverStatsPeriodTwo := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, serverStatsPeriodOne, serverStatsPeriodDBTypes, false, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}
	if err = randomize.Struct(seed, serverStatsPeriodTwo, serverStatsPeriodDBTypes, false, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = serverStatsPeriodOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = serverStatsPeriodTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testServerStatsPeriodsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testServerStatsPeriodsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(serverStatsPeriodColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testServerStatsPeriodsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testServerStatsPeriodsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := ServerStatsPeriodSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testServerStatsPeriodsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := ServerStatsPeriods().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	serverStatsPeriodDBTypes = map[string]string{`ChannelID`: `bigint`, `Count`: `bigint`, `Duration`: `bigint`, `GuildID`: `bigint`, `ID`: `bigint`, `Started`: `timestamp with time zone`, `UserID`: `bigint`}
	_                        = bytes.MinRead
)

func testServerStatsPeriodsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(serverStatsPeriodPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(serverStatsPeriodColumns) == len(serverStatsPeriodPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testServerStatsPeriodsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(serverStatsPeriodColumns) == len(serverStatsPeriodPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &ServerStatsPeriod{}
	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, serverStatsPeriodDBTypes, true, serverStatsPeriodPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(serverStatsPeriodColumns, serverStatsPeriodPrimaryKeyColumns) {
		fields = serverStatsPeriodColumns
	} else {
		fields = strmangle.SetComplement(
			serverStatsPeriodColumns,
			serverStatsPeriodPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := ServerStatsPeriodSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testServerStatsPeriodsUpsert(t *testing.T) {
	t.Parallel()

	if len(serverStatsPeriodColumns) == len(serverStatsPeriodPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := ServerStatsPeriod{}
	if err = randomize.Struct(seed, &o, serverStatsPeriodDBTypes, true); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ServerStatsPeriod: %s", err)
	}

	count, err := ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, serverStatsPeriodDBTypes, false, serverStatsPeriodPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize ServerStatsPeriod struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert ServerStatsPeriod: %s", err)
	}

	count, err = ServerStatsPeriods().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
