// Code generated by SQLBoiler 4.16.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/v4/types"
	"github.com/volatiletech/strmangle"
)

// TicketConfig is an object representing the database table.
type TicketConfig struct {
	GuildID                            int64            `boil:"guild_id" json:"guild_id" toml:"guild_id" yaml:"guild_id"`
	Enabled                            bool             `boil:"enabled" json:"enabled" toml:"enabled" yaml:"enabled"`
	TicketOpenMSG                      string           `boil:"ticket_open_msg" json:"ticket_open_msg" toml:"ticket_open_msg" yaml:"ticket_open_msg"`
	TicketsChannelCategory             int64            `boil:"tickets_channel_category" json:"tickets_channel_category" toml:"tickets_channel_category" yaml:"tickets_channel_category"`
	StatusChannel                      int64            `boil:"status_channel" json:"status_channel" toml:"status_channel" yaml:"status_channel"`
	TicketsTranscriptsChannel          int64            `boil:"tickets_transcripts_channel" json:"tickets_transcripts_channel" toml:"tickets_transcripts_channel" yaml:"tickets_transcripts_channel"`
	DownloadAttachments                bool             `boil:"download_attachments" json:"download_attachments" toml:"download_attachments" yaml:"download_attachments"`
	TicketsUseTXTTranscripts           bool             `boil:"tickets_use_txt_transcripts" json:"tickets_use_txt_transcripts" toml:"tickets_use_txt_transcripts" yaml:"tickets_use_txt_transcripts"`
	ModRoles                           types.Int64Array `boil:"mod_roles" json:"mod_roles,omitempty" toml:"mod_roles" yaml:"mod_roles,omitempty"`
	AdminRoles                         types.Int64Array `boil:"admin_roles" json:"admin_roles,omitempty" toml:"admin_roles" yaml:"admin_roles,omitempty"`
	TicketsTranscriptsChannelAdminOnly int64            `boil:"tickets_transcripts_channel_admin_only" json:"tickets_transcripts_channel_admin_only" toml:"tickets_transcripts_channel_admin_only" yaml:"tickets_transcripts_channel_admin_only"`
	AppendButtons                      int64            `boil:"append_buttons" json:"append_buttons" toml:"append_buttons" yaml:"append_buttons"`

	R *ticketConfigR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L ticketConfigL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TicketConfigColumns = struct {
	GuildID                            string
	Enabled                            string
	TicketOpenMSG                      string
	TicketsChannelCategory             string
	StatusChannel                      string
	TicketsTranscriptsChannel          string
	DownloadAttachments                string
	TicketsUseTXTTranscripts           string
	ModRoles                           string
	AdminRoles                         string
	TicketsTranscriptsChannelAdminOnly string
	AppendButtons                      string
}{
	GuildID:                            "guild_id",
	Enabled:                            "enabled",
	TicketOpenMSG:                      "ticket_open_msg",
	TicketsChannelCategory:             "tickets_channel_category",
	StatusChannel:                      "status_channel",
	TicketsTranscriptsChannel:          "tickets_transcripts_channel",
	DownloadAttachments:                "download_attachments",
	TicketsUseTXTTranscripts:           "tickets_use_txt_transcripts",
	ModRoles:                           "mod_roles",
	AdminRoles:                         "admin_roles",
	TicketsTranscriptsChannelAdminOnly: "tickets_transcripts_channel_admin_only",
	AppendButtons:                      "append_buttons",
}

var TicketConfigTableColumns = struct {
	GuildID                            string
	Enabled                            string
	TicketOpenMSG                      string
	TicketsChannelCategory             string
	StatusChannel                      string
	TicketsTranscriptsChannel          string
	DownloadAttachments                string
	TicketsUseTXTTranscripts           string
	ModRoles                           string
	AdminRoles                         string
	TicketsTranscriptsChannelAdminOnly string
	AppendButtons                      string
}{
	GuildID:                            "ticket_configs.guild_id",
	Enabled:                            "ticket_configs.enabled",
	TicketOpenMSG:                      "ticket_configs.ticket_open_msg",
	TicketsChannelCategory:             "ticket_configs.tickets_channel_category",
	StatusChannel:                      "ticket_configs.status_channel",
	TicketsTranscriptsChannel:          "ticket_configs.tickets_transcripts_channel",
	DownloadAttachments:                "ticket_configs.download_attachments",
	TicketsUseTXTTranscripts:           "ticket_configs.tickets_use_txt_transcripts",
	ModRoles:                           "ticket_configs.mod_roles",
	AdminRoles:                         "ticket_configs.admin_roles",
	TicketsTranscriptsChannelAdminOnly: "ticket_configs.tickets_transcripts_channel_admin_only",
	AppendButtons:                      "ticket_configs.append_buttons",
}

// Generated where

type whereHelperint64 struct{ field string }

func (w whereHelperint64) EQ(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperint64) NEQ(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperint64) LT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperint64) LTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperint64) GT(x int64) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperint64) GTE(x int64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperint64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperint64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelperbool struct{ field string }

func (w whereHelperbool) EQ(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperbool) NEQ(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperbool) LT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperbool) LTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperbool) GT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperbool) GTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }

type whereHelperstring struct{ field string }

func (w whereHelperstring) EQ(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperstring) NEQ(x string) qm.QueryMod    { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperstring) LT(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperstring) LTE(x string) qm.QueryMod    { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperstring) GT(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperstring) GTE(x string) qm.QueryMod    { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperstring) LIKE(x string) qm.QueryMod   { return qm.Where(w.field+" LIKE ?", x) }
func (w whereHelperstring) NLIKE(x string) qm.QueryMod  { return qm.Where(w.field+" NOT LIKE ?", x) }
func (w whereHelperstring) ILIKE(x string) qm.QueryMod  { return qm.Where(w.field+" ILIKE ?", x) }
func (w whereHelperstring) NILIKE(x string) qm.QueryMod { return qm.Where(w.field+" NOT ILIKE ?", x) }
func (w whereHelperstring) IN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperstring) NIN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelpertypes_Int64Array struct{ field string }

func (w whereHelpertypes_Int64Array) EQ(x types.Int64Array) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpertypes_Int64Array) NEQ(x types.Int64Array) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpertypes_Int64Array) LT(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_Int64Array) LTE(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_Int64Array) GT(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_Int64Array) GTE(x types.Int64Array) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpertypes_Int64Array) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpertypes_Int64Array) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var TicketConfigWhere = struct {
	GuildID                            whereHelperint64
	Enabled                            whereHelperbool
	TicketOpenMSG                      whereHelperstring
	TicketsChannelCategory             whereHelperint64
	StatusChannel                      whereHelperint64
	TicketsTranscriptsChannel          whereHelperint64
	DownloadAttachments                whereHelperbool
	TicketsUseTXTTranscripts           whereHelperbool
	ModRoles                           whereHelpertypes_Int64Array
	AdminRoles                         whereHelpertypes_Int64Array
	TicketsTranscriptsChannelAdminOnly whereHelperint64
	AppendButtons                      whereHelperint64
}{
	GuildID:                            whereHelperint64{field: "\"ticket_configs\".\"guild_id\""},
	Enabled:                            whereHelperbool{field: "\"ticket_configs\".\"enabled\""},
	TicketOpenMSG:                      whereHelperstring{field: "\"ticket_configs\".\"ticket_open_msg\""},
	TicketsChannelCategory:             whereHelperint64{field: "\"ticket_configs\".\"tickets_channel_category\""},
	StatusChannel:                      whereHelperint64{field: "\"ticket_configs\".\"status_channel\""},
	TicketsTranscriptsChannel:          whereHelperint64{field: "\"ticket_configs\".\"tickets_transcripts_channel\""},
	DownloadAttachments:                whereHelperbool{field: "\"ticket_configs\".\"download_attachments\""},
	TicketsUseTXTTranscripts:           whereHelperbool{field: "\"ticket_configs\".\"tickets_use_txt_transcripts\""},
	ModRoles:                           whereHelpertypes_Int64Array{field: "\"ticket_configs\".\"mod_roles\""},
	AdminRoles:                         whereHelpertypes_Int64Array{field: "\"ticket_configs\".\"admin_roles\""},
	TicketsTranscriptsChannelAdminOnly: whereHelperint64{field: "\"ticket_configs\".\"tickets_transcripts_channel_admin_only\""},
	AppendButtons:                      whereHelperint64{field: "\"ticket_configs\".\"append_buttons\""},
}

// TicketConfigRels is where relationship names are stored.
var TicketConfigRels = struct {
}{}

// ticketConfigR is where relationships are stored.
type ticketConfigR struct {
}

// NewStruct creates a new relationship struct
func (*ticketConfigR) NewStruct() *ticketConfigR {
	return &ticketConfigR{}
}

// ticketConfigL is where Load methods for each relationship are stored.
type ticketConfigL struct{}

var (
	ticketConfigAllColumns            = []string{"guild_id", "enabled", "ticket_open_msg", "tickets_channel_category", "status_channel", "tickets_transcripts_channel", "download_attachments", "tickets_use_txt_transcripts", "mod_roles", "admin_roles", "tickets_transcripts_channel_admin_only", "append_buttons"}
	ticketConfigColumnsWithoutDefault = []string{"guild_id", "enabled", "ticket_open_msg", "tickets_channel_category", "status_channel", "tickets_transcripts_channel", "download_attachments", "tickets_use_txt_transcripts"}
	ticketConfigColumnsWithDefault    = []string{"mod_roles", "admin_roles", "tickets_transcripts_channel_admin_only", "append_buttons"}
	ticketConfigPrimaryKeyColumns     = []string{"guild_id"}
	ticketConfigGeneratedColumns      = []string{}
)

type (
	// TicketConfigSlice is an alias for a slice of pointers to TicketConfig.
	// This should almost always be used instead of []TicketConfig.
	TicketConfigSlice []*TicketConfig

	ticketConfigQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	ticketConfigType                 = reflect.TypeOf(&TicketConfig{})
	ticketConfigMapping              = queries.MakeStructMapping(ticketConfigType)
	ticketConfigPrimaryKeyMapping, _ = queries.BindMapping(ticketConfigType, ticketConfigMapping, ticketConfigPrimaryKeyColumns)
	ticketConfigInsertCacheMut       sync.RWMutex
	ticketConfigInsertCache          = make(map[string]insertCache)
	ticketConfigUpdateCacheMut       sync.RWMutex
	ticketConfigUpdateCache          = make(map[string]updateCache)
	ticketConfigUpsertCacheMut       sync.RWMutex
	ticketConfigUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// OneG returns a single ticketConfig record from the query using the global executor.
func (q ticketConfigQuery) OneG(ctx context.Context) (*TicketConfig, error) {
	return q.One(ctx, boil.GetContextDB())
}

// One returns a single ticketConfig record from the query.
func (q ticketConfigQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TicketConfig, error) {
	o := &TicketConfig{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for ticket_configs")
	}

	return o, nil
}

// AllG returns all TicketConfig records from the query using the global executor.
func (q ticketConfigQuery) AllG(ctx context.Context) (TicketConfigSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// All returns all TicketConfig records from the query.
func (q ticketConfigQuery) All(ctx context.Context, exec boil.ContextExecutor) (TicketConfigSlice, error) {
	var o []*TicketConfig

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TicketConfig slice")
	}

	return o, nil
}

// CountG returns the count of all TicketConfig records in the query using the global executor
func (q ticketConfigQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// Count returns the count of all TicketConfig records in the query.
func (q ticketConfigQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count ticket_configs rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q ticketConfigQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// Exists checks if the row exists in the table.
func (q ticketConfigQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if ticket_configs exists")
	}

	return count > 0, nil
}

// TicketConfigs retrieves all the records using an executor.
func TicketConfigs(mods ...qm.QueryMod) ticketConfigQuery {
	mods = append(mods, qm.From("\"ticket_configs\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"ticket_configs\".*"})
	}

	return ticketConfigQuery{q}
}

// FindTicketConfigG retrieves a single record by ID.
func FindTicketConfigG(ctx context.Context, guildID int64, selectCols ...string) (*TicketConfig, error) {
	return FindTicketConfig(ctx, boil.GetContextDB(), guildID, selectCols...)
}

// FindTicketConfig retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTicketConfig(ctx context.Context, exec boil.ContextExecutor, guildID int64, selectCols ...string) (*TicketConfig, error) {
	ticketConfigObj := &TicketConfig{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"ticket_configs\" where \"guild_id\"=$1", sel,
	)

	q := queries.Raw(query, guildID)

	err := q.Bind(ctx, exec, ticketConfigObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from ticket_configs")
	}

	return ticketConfigObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TicketConfig) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TicketConfig) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no ticket_configs provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(ticketConfigColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	ticketConfigInsertCacheMut.RLock()
	cache, cached := ticketConfigInsertCache[key]
	ticketConfigInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			ticketConfigAllColumns,
			ticketConfigColumnsWithDefault,
			ticketConfigColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(ticketConfigType, ticketConfigMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(ticketConfigType, ticketConfigMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"ticket_configs\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"ticket_configs\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into ticket_configs")
	}

	if !cached {
		ticketConfigInsertCacheMut.Lock()
		ticketConfigInsertCache[key] = cache
		ticketConfigInsertCacheMut.Unlock()
	}

	return nil
}

// UpdateG a single TicketConfig record using the global executor.
// See Update for more documentation.
func (o *TicketConfig) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// Update uses an executor to update the TicketConfig.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TicketConfig) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	ticketConfigUpdateCacheMut.RLock()
	cache, cached := ticketConfigUpdateCache[key]
	ticketConfigUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			ticketConfigAllColumns,
			ticketConfigPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update ticket_configs, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"ticket_configs\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, ticketConfigPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(ticketConfigType, ticketConfigMapping, append(wl, ticketConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update ticket_configs row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for ticket_configs")
	}

	if !cached {
		ticketConfigUpdateCacheMut.Lock()
		ticketConfigUpdateCache[key] = cache
		ticketConfigUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (q ticketConfigQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values.
func (q ticketConfigQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for ticket_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for ticket_configs")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TicketConfigSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TicketConfigSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ticketConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"ticket_configs\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, ticketConfigPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in ticketConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all ticketConfig")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TicketConfig) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TicketConfig) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no ticket_configs provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(ticketConfigColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	ticketConfigUpsertCacheMut.RLock()
	cache, cached := ticketConfigUpsertCache[key]
	ticketConfigUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			ticketConfigAllColumns,
			ticketConfigColumnsWithDefault,
			ticketConfigColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			ticketConfigAllColumns,
			ticketConfigPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert ticket_configs, could not build update column list")
		}

		ret := strmangle.SetComplement(ticketConfigAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(ticketConfigPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert ticket_configs, could not build conflict column list")
			}

			conflict = make([]string, len(ticketConfigPrimaryKeyColumns))
			copy(conflict, ticketConfigPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"ticket_configs\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(ticketConfigType, ticketConfigMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(ticketConfigType, ticketConfigMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert ticket_configs")
	}

	if !cached {
		ticketConfigUpsertCacheMut.Lock()
		ticketConfigUpsertCache[key] = cache
		ticketConfigUpsertCacheMut.Unlock()
	}

	return nil
}

// DeleteG deletes a single TicketConfig record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TicketConfig) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// Delete deletes a single TicketConfig record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TicketConfig) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TicketConfig provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), ticketConfigPrimaryKeyMapping)
	sql := "DELETE FROM \"ticket_configs\" WHERE \"guild_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from ticket_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for ticket_configs")
	}

	return rowsAff, nil
}

func (q ticketConfigQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all matching rows.
func (q ticketConfigQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no ticketConfigQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ticket_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ticket_configs")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TicketConfigSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TicketConfigSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ticketConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"ticket_configs\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, ticketConfigPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from ticketConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for ticket_configs")
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TicketConfig) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: no TicketConfig provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TicketConfig) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTicketConfig(ctx, exec, o.GuildID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TicketConfigSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("models: empty TicketConfigSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TicketConfigSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TicketConfigSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), ticketConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"ticket_configs\".* FROM \"ticket_configs\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, ticketConfigPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TicketConfigSlice")
	}

	*o = slice

	return nil
}

// TicketConfigExistsG checks if the TicketConfig row exists.
func TicketConfigExistsG(ctx context.Context, guildID int64) (bool, error) {
	return TicketConfigExists(ctx, boil.GetContextDB(), guildID)
}

// TicketConfigExists checks if the TicketConfig row exists.
func TicketConfigExists(ctx context.Context, exec boil.ContextExecutor, guildID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"ticket_configs\" where \"guild_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, guildID)
	}
	row := exec.QueryRowContext(ctx, sql, guildID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if ticket_configs exists")
	}

	return exists, nil
}

// Exists checks if the TicketConfig row exists.
func (o *TicketConfig) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TicketConfigExists(ctx, exec, o.GuildID)
}
