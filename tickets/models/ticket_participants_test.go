// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testTicketParticipants(t *testing.T) {
	t.Parallel()

	query := TicketParticipants()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testTicketParticipantsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTicketParticipantsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := TicketParticipants().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTicketParticipantsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TicketParticipantSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTicketParticipantsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := TicketParticipantExists(ctx, tx, o.TicketGuildID, o.TicketLocalID, o.UserID)
	if err != nil {
		t.Errorf("Unable to check if TicketParticipant exists: %s", err)
	}
	if !e {
		t.Errorf("Expected TicketParticipantExists to return true, but got false.")
	}
}

func testTicketParticipantsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	ticketParticipantFound, err := FindTicketParticipant(ctx, tx, o.TicketGuildID, o.TicketLocalID, o.UserID)
	if err != nil {
		t.Error(err)
	}

	if ticketParticipantFound == nil {
		t.Error("want a record, got nil")
	}
}

func testTicketParticipantsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = TicketParticipants().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testTicketParticipantsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := TicketParticipants().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testTicketParticipantsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	ticketParticipantOne := &TicketParticipant{}
	ticketParticipantTwo := &TicketParticipant{}
	if err = randomize.Struct(seed, ticketParticipantOne, ticketParticipantDBTypes, false, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}
	if err = randomize.Struct(seed, ticketParticipantTwo, ticketParticipantDBTypes, false, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = ticketParticipantOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = ticketParticipantTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := TicketParticipants().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testTicketParticipantsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	ticketParticipantOne := &TicketParticipant{}
	ticketParticipantTwo := &TicketParticipant{}
	if err = randomize.Struct(seed, ticketParticipantOne, ticketParticipantDBTypes, false, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}
	if err = randomize.Struct(seed, ticketParticipantTwo, ticketParticipantDBTypes, false, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = ticketParticipantOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = ticketParticipantTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testTicketParticipantsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTicketParticipantsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(ticketParticipantColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTicketParticipantsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTicketParticipantsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TicketParticipantSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTicketParticipantsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := TicketParticipants().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	ticketParticipantDBTypes = map[string]string{`TicketGuildID`: `bigint`, `TicketLocalID`: `bigint`, `UserID`: `bigint`, `Username`: `text`, `Discrim`: `text`, `IsStaff`: `boolean`}
	_                        = bytes.MinRead
)

func testTicketParticipantsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(ticketParticipantPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(ticketParticipantColumns) == len(ticketParticipantPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testTicketParticipantsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(ticketParticipantColumns) == len(ticketParticipantPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &TicketParticipant{}
	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, ticketParticipantDBTypes, true, ticketParticipantPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(ticketParticipantColumns, ticketParticipantPrimaryKeyColumns) {
		fields = ticketParticipantColumns
	} else {
		fields = strmangle.SetComplement(
			ticketParticipantColumns,
			ticketParticipantPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := TicketParticipantSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testTicketParticipantsUpsert(t *testing.T) {
	t.Parallel()

	if len(ticketParticipantColumns) == len(ticketParticipantPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := TicketParticipant{}
	if err = randomize.Struct(seed, &o, ticketParticipantDBTypes, true); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert TicketParticipant: %s", err)
	}

	count, err := TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, ticketParticipantDBTypes, false, ticketParticipantPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize TicketParticipant struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert TicketParticipant: %s", err)
	}

	count, err = TicketParticipants().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
